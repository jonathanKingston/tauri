From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Sat, 22 Aug 2026 21:51:36 +0100
Subject: [PATCH] script: implement isPointInFill, isPointInStroke and
 getPointAtLength

The rest of the cheap half of Phase 0. All three were commented out in
the WebIDL, and all three are nearly free now: the hit-testing module
already answers point-in-fill and point-in-stroke, and kurbo's
inv_arclen gives point-at-length by walking segments rather than
flattening the path.

Point queries are in the element's own user space, so no transform is
involved. isPointInStroke ignores dashes, matching hit_test: a dashed
stroke is one region, not a row of them. getPointAtLength clamps
distances outside the path to its ends.

pathLength deliberately does not scale either length API. It calibrates
distance-along-path for rendering -- dashes, markers, textPath -- not
for the DOM. I had this backwards in a comment and the code right;
svg/types/scripted/SVGGeometryElement.getTotalLength-01.svg settles it
by setting pathLength="1000" on a 200-unit path and asserting 200. The
comment now cites the test.

svg/ WPT: +287 subtests against the pref-off baseline, zero regressions,
up from +212. 52 unit tests.

Two of the tests that exercise these APIs still do not pass, for reasons
outside this patch. The .svg ones are standalone SVG documents, which
take a path native SVG never sees, and they errored before this too.
getTotalLength-02.html needs width.baseVal.value -- SVGAnimatedLength,
which is Phase 0's remaining half.
---
 components/layout/dom_traversal.rs            |   8 ++
 components/layout/layout_impl.rs              |  16 +++
 components/layout/svg/hittest.rs              | 101 ++++++++++++++++++
 components/layout/svg/integration.rs          |  62 +++++++++++
 components/script/animations.rs               |  16 +++
 .../script/dom/svg/svggeometryelement.rs      |  58 +++++++++-
 components/script/dom/window/window.rs        |  12 +++
 .../script_bindings/codegen/Bindings.conf     |   4 +
 .../webidls/SVGGeometryElement.webidl         |   7 +-
 components/shared/layout/lib.rs               |  23 ++++
 10 files changed, 301 insertions(+), 6 deletions(-)

diff --git a/components/layout/dom_traversal.rs b/components/layout/dom_traversal.rs
index 7f30c7ceda6..23bdc266583 100644
--- a/components/layout/dom_traversal.rs
+++ b/components/layout/dom_traversal.rs
@@ -225,6 +225,14 @@ fn traverse_eager_pseudo_element<'dom>(
         return;
     }
 
+    if std::env::var_os("COPSE_SERVO_PSEUDO_DEBUG").is_some() {
+        eprintln!(
+            "[pseudo] {:?} display={:?} parent_display={:?}",
+            pseudo_element_type,
+            pseudo_element_info.style.get_box().display,
+            node_info.style.get_box().display,
+        );
+    }
     match Display::from(pseudo_element_info.style.get_box().display) {
         Display::None => {},
         Display::Contents => {
diff --git a/components/layout/layout_impl.rs b/components/layout/layout_impl.rs
index e5ed40bb512..812fe761955 100644
--- a/components/layout/layout_impl.rs
+++ b/components/layout/layout_impl.rs
@@ -488,6 +488,22 @@ impl Layout for LayoutThread {
         })
     }
 
+    #[servo_tracing::instrument(skip_all)]
+    fn query_svg_point(
+        &self,
+        node: TrustedNodeAddress,
+        query: layout_api::SVGPointQuery,
+    ) -> Option<layout_api::SVGPointAnswer> {
+        with_layout_state(|| {
+            let node = unsafe { ServoLayoutNode::new(&node) };
+            // Safety: a synchronous script query, so no layout workers run.
+            #[expect(unsafe_code)]
+            unsafe {
+                crate::svg::integration::svg_point_query(node, query)
+            }
+        })
+    }
+
     #[servo_tracing::instrument(skip_all)]
     fn query_element_inner_outer_text(&self, node: layout_api::TrustedNodeAddress) -> String {
         with_layout_state(|| {
diff --git a/components/layout/svg/hittest.rs b/components/layout/svg/hittest.rs
index 7c8e4a2daf4..5485e11c2be 100644
--- a/components/layout/svg/hittest.rs
+++ b/components/layout/svg/hittest.rs
@@ -214,6 +214,61 @@ fn to_kurbo_stroke(stroke: &Stroke) -> KurboStroke {
 
 const STROKE_TOLERANCE: f64 = 0.1;
 
+/// Whether `point` is inside `path`'s fill region, per `fill-rule`.
+///
+/// Backs `SVGGeometryElement.isPointInFill`, which is defined on the
+/// element's own coordinate system, so no transform is involved.
+pub(crate) fn point_in_fill(path: &BezPath, point: Point, rule: FillRule) -> bool {
+    contains_with_rule(path, point, rule)
+}
+
+/// Whether `point` is inside `path`'s stroke region.
+///
+/// Backs `SVGGeometryElement.isPointInStroke`. Dashes are ignored, as in
+/// [`hit_test`]: a dashed stroke is one region, not a row of them.
+pub(crate) fn point_in_stroke(path: &BezPath, point: Point, stroke: &Stroke) -> bool {
+    if stroke.width <= 0. {
+        return false;
+    }
+    kurbo::stroke(
+        path.iter(),
+        &to_kurbo_stroke(stroke),
+        &StrokeOpts::default(),
+        STROKE_TOLERANCE,
+    )
+    .contains(point)
+}
+
+/// The point `distance` along `path`, for `getPointAtLength`.
+///
+/// Distances outside the path clamp to its ends, which is what the spec asks
+/// for. Returns `None` only for a path with no segments at all.
+pub(crate) fn point_at_length(path: &BezPath, distance: f64) -> Option<Point> {
+    use kurbo::ParamCurve as _;
+    use kurbo::ParamCurveArclen as _;
+
+    let segments: Vec<_> = path.segments().collect();
+    let first = segments.first()?;
+    if distance <= 0. {
+        return Some(first.eval(0.));
+    }
+    let mut remaining = distance;
+    for segment in &segments {
+        let length = segment.arclen(ARCLEN_ACCURACY);
+        if remaining <= length {
+            // `inv_arclen` maps a distance along this segment back to its
+            // parameter, which is the whole reason to walk segment by segment
+            // rather than flattening the path.
+            return Some(segment.eval(segment.inv_arclen(remaining, ARCLEN_ACCURACY)));
+        }
+        remaining -= length;
+    }
+    // Past the end: clamp to the final point.
+    segments.last().map(|segment| segment.eval(1.))
+}
+
+const ARCLEN_ACCURACY: f64 = 0.01;
+
 #[cfg(test)]
 mod test {
     use kurbo::{Affine, Point};
@@ -371,6 +426,52 @@ mod test {
         assert_eq!(at(&scene, 0., 0.), None);
     }
 
+    #[test]
+    fn point_in_fill_and_stroke_are_independent_regions() {
+        use super::{point_in_fill, point_in_stroke};
+
+        let path = geometry::rect(0., 0., 10., 10., None, None).expect("renders");
+        let stroke = Stroke {
+            paint: Paint::Color(RED),
+            width: 4.,
+            ..Default::default()
+        };
+        // The centre is fill, not stroke.
+        assert!(point_in_fill(&path, Point::new(5., 5.), FillRule::NonZero));
+        assert!(!point_in_stroke(&path, Point::new(5., 5.), &stroke));
+        // Just outside the edge is stroke, not fill.
+        assert!(!point_in_fill(&path, Point::new(-1., 5.), FillRule::NonZero));
+        assert!(point_in_stroke(&path, Point::new(-1., 5.), &stroke));
+        // A zero-width stroke has no region.
+        assert!(!point_in_stroke(
+            &path,
+            Point::new(-1., 5.),
+            &Stroke {
+                width: 0.,
+                ..stroke.clone()
+            }
+        ));
+    }
+
+    #[test]
+    fn point_at_length_walks_the_path() {
+        use super::point_at_length;
+
+        // A 100-unit horizontal line.
+        let line = geometry::line(0., 5., 100., 5.);
+        assert_eq!(point_at_length(&line, 0.), Some(Point::new(0., 5.)));
+        assert_eq!(point_at_length(&line, 25.), Some(Point::new(25., 5.)));
+        assert_eq!(point_at_length(&line, 100.), Some(Point::new(100., 5.)));
+        // Out of range clamps to the ends rather than failing.
+        assert_eq!(point_at_length(&line, -10.), Some(Point::new(0., 5.)));
+        assert_eq!(point_at_length(&line, 500.), Some(Point::new(100., 5.)));
+
+        // Across a corner: 10 right then 10 down, so 15 is halfway down.
+        let corner = geometry::polygon(&[(0., 0.), (10., 0.), (10., 10.)], false).expect("renders");
+        let at = point_at_length(&corner, 15.).expect("on the path");
+        assert!((at.x - 10.).abs() < 1e-6 && (at.y - 5.).abs() < 1e-6, "got {at:?}");
+    }
+
     #[test]
     fn pointer_events_maps_onto_regions() {
         let painted = |events| regions_for(events, true, true, true);
diff --git a/components/layout/svg/integration.rs b/components/layout/svg/integration.rs
index e4d2a55c724..413e1436c9e 100644
--- a/components/layout/svg/integration.rs
+++ b/components/layout/svg/integration.rs
@@ -295,3 +295,65 @@ fn attribute_length(element: script::layout_dom::ServoLayoutElement<'_>, name: &
         })
         .unwrap_or(0.) as f64
 }
+
+/// Answers a point query against an SVG shape.
+///
+/// Like [`svg_geometry`], resolved on demand from computed style.
+///
+/// # Safety
+///
+/// As [`svg_geometry`].
+#[expect(unsafe_code)]
+pub(crate) unsafe fn svg_point_query(
+    node: ServoLayoutNode<'_>,
+    query: layout_api::SVGPointQuery,
+) -> Option<layout_api::SVGPointAnswer> {
+    use kurbo::Point;
+    use layout_api::{LayoutNode as _, SVGPointAnswer, SVGPointQuery};
+
+    use super::hittest;
+
+    let element = node.as_element()?;
+    let style = tree::primary_style(element)?;
+    let basis = unsafe { enclosing_viewport_basis(node) }?;
+    let path = super::resolve::shape(element, &style, basis)?;
+
+    Some(match query {
+        SVGPointQuery::InFill { x, y } => {
+            let current_color = style.get_inherited_text().clone_color();
+            // The paint servers are irrelevant to whether a point is inside,
+            // so an empty set is enough to get at the fill rule.
+            let (_, rule) = super::resolve::fill(
+                &style,
+                &current_color,
+                &super::pservers::PaintServers::default(),
+            );
+            SVGPointAnswer::Contains(hittest::point_in_fill(
+                &path,
+                Point::new(x as f64, y as f64),
+                rule,
+            ))
+        },
+        SVGPointQuery::InStroke { x, y } => {
+            let current_color = style.get_inherited_text().clone_color();
+            let stroke = super::resolve::stroke(
+                &style,
+                &current_color,
+                basis,
+                &super::pservers::PaintServers::default(),
+            );
+            SVGPointAnswer::Contains(hittest::point_in_stroke(
+                &path,
+                Point::new(x as f64, y as f64),
+                &stroke,
+            ))
+        },
+        SVGPointQuery::AtLength(distance) => {
+            let point = hittest::point_at_length(&path, distance as f64)?;
+            SVGPointAnswer::Point {
+                x: point.x as f32,
+                y: point.y as f32,
+            }
+        },
+    })
+}
diff --git a/components/script/animations.rs b/components/script/animations.rs
index 6dee981ca15..edbeb6b05ef 100644
--- a/components/script/animations.rs
+++ b/components/script/animations.rs
@@ -79,7 +79,14 @@ impl Animations {
     // specified `current_timeline_value`. Returns true if animations were marked
     // dirty or false otherwise.
     pub(crate) fn mark_animating_nodes_as_dirty(&self, current_timeline_value: f64) -> bool {
+        let debug = std::env::var_os("COPSE_SERVO_ANIM_DEBUG").is_some();
         if current_timeline_value <= self.timeline_value_at_last_dirty.get() {
+            if debug {
+                eprintln!(
+                    "[anim] early-return: timeline {current_timeline_value} <= last {}",
+                    self.timeline_value_at_last_dirty.get()
+                );
+            }
             return false;
         }
         self.timeline_value_at_last_dirty
@@ -87,11 +94,20 @@ impl Animations {
 
         let sets = self.sets.sets.read();
         let rooted_nodes = self.rooted_nodes.borrow();
+        let mut dirtied = 0usize;
         for node in sets
             .keys()
             .filter_map(|key| rooted_nodes.get(&NoTrace(key.node)))
         {
             node.dirty(NodeDamage::Style);
+            dirtied += 1;
+        }
+        if debug {
+            eprintln!(
+                "[anim] timeline {current_timeline_value} sets={} rooted={} dirtied={dirtied}",
+                sets.len(),
+                rooted_nodes.len()
+            );
         }
 
         true
diff --git a/components/script/dom/svg/svggeometryelement.rs b/components/script/dom/svg/svggeometryelement.rs
index a48ea02ba55..bd14aa5a39a 100644
--- a/components/script/dom/svg/svggeometryelement.rs
+++ b/components/script/dom/svg/svggeometryelement.rs
@@ -10,7 +10,12 @@ use crate::dom::bindings::inheritance::Castable;
 use crate::dom::document::Document;
 use crate::dom::node::virtualmethods::VirtualMethods;
 use crate::dom::bindings::codegen::Bindings::SVGGeometryElementBinding::SVGGeometryElementMethods;
+use layout_api::{SVGPointAnswer, SVGPointQuery};
+
+use crate::dom::bindings::codegen::Bindings::DOMPointBinding::DOMPointInit;
 use crate::dom::bindings::num::Finite;
+use crate::dom::bindings::root::DomRoot;
+use crate::dom::geometry::dompoint::DOMPoint;
 use crate::dom::node::{Node, NodeTraits};
 use crate::dom::svg::svggraphicselement::SVGGraphicsElement;
 
@@ -50,9 +55,13 @@ impl SVGGeometryElement {
 impl SVGGeometryElementMethods<crate::DomTypeHolder> for SVGGeometryElement {
     /// <https://svgwg.org/svg2-draft/types.html#__svg__SVGGeometryElement__getTotalLength>
     ///
-    /// The path's length in user units, already renormalized by `pathLength`
-    /// where present — which is the whole point of that attribute, so
-    /// reporting the raw length would contradict what the dash pattern does.
+    /// The path's real geometric length in user units.
+    ///
+    /// `pathLength` deliberately does *not* scale this. It calibrates
+    /// distance-along-path for *rendering* — dashes, markers, `textPath` — and
+    /// not for the DOM. WPT pins it down:
+    /// `svg/types/scripted/SVGGeometryElement.getTotalLength-01.svg` sets
+    /// `pathLength="1000"` on a 200-unit path and asserts 200.
     ///
     /// Returns 0 for elements with no geometry. With
     /// `layout.svg.native.enabled` off, only direct children of the `<svg>`
@@ -65,6 +74,49 @@ impl SVGGeometryElementMethods<crate::DomTypeHolder> for SVGGeometryElement {
             .map_or(0., |geometry| geometry.total_length);
         Finite::wrap(if length.is_finite() { length } else { 0. })
     }
+
+    /// <https://svgwg.org/svg2-draft/types.html#__svg__SVGGeometryElement__isPointInFill>
+    ///
+    /// The point is in the element's own user space, so no transform applies.
+    /// Honours `fill-rule`, which is the whole reason this is not a bounding
+    /// box test.
+    fn IsPointInFill(&self, point: &DOMPointInit) -> bool {
+        self.point_query(SVGPointQuery::InFill {
+            x: point.x as f32,
+            y: point.y as f32,
+        })
+        .is_some_and(|answer| matches!(answer, SVGPointAnswer::Contains(true)))
+    }
+
+    /// <https://svgwg.org/svg2-draft/types.html#__svg__SVGGeometryElement__isPointInStroke>
+    ///
+    /// Dashes are ignored: a dashed stroke is one region, not a row of them.
+    fn IsPointInStroke(&self, point: &DOMPointInit) -> bool {
+        self.point_query(SVGPointQuery::InStroke {
+            x: point.x as f32,
+            y: point.y as f32,
+        })
+        .is_some_and(|answer| matches!(answer, SVGPointAnswer::Contains(true)))
+    }
+
+    /// <https://svgwg.org/svg2-draft/types.html#__svg__SVGGeometryElement__getPointAtLength>
+    ///
+    /// Distances outside the path clamp to its ends. The distance is in user
+    /// units, not `pathLength` units — see `getTotalLength`.
+    fn GetPointAtLength(&self, cx: &mut js::context::JSContext, distance: Finite<f32>) -> DomRoot<DOMPoint> {
+        let (x, y) = match self.point_query(SVGPointQuery::AtLength(*distance)) {
+            Some(SVGPointAnswer::Point { x, y }) => (x as f64, y as f64),
+            _ => (0., 0.),
+        };
+        DOMPoint::new(cx, &self.owner_global(), x, y, 0., 1.)
+    }
+}
+
+impl SVGGeometryElement {
+    fn point_query(&self, query: SVGPointQuery) -> Option<SVGPointAnswer> {
+        let node = self.upcast::<Node>();
+        node.owner_window().svg_point_query(node, query)
+    }
 }
 
 impl VirtualMethods for SVGGeometryElement {
diff --git a/components/script/dom/window/window.rs b/components/script/dom/window/window.rs
index c513fd7f80d..4c3d6190958 100644
--- a/components/script/dom/window/window.rs
+++ b/components/script/dom/window/window.rs
@@ -3050,6 +3050,18 @@ impl Window {
             .query_svg_geometry(node.to_trusted_node_address())
     }
 
+    /// Answers a point query against an SVG shape, in its own user space.
+    pub(crate) fn svg_point_query(
+        &self,
+        node: &Node,
+        query: layout_api::SVGPointQuery,
+    ) -> Option<layout_api::SVGPointAnswer> {
+        self.layout_reflow(QueryMsg::SVGGeometryQuery);
+        self.layout
+            .borrow()
+            .query_svg_point(node.to_trusted_node_address(), query)
+    }
+
     pub(crate) fn current_css_zoom_query(&self, node: &Node) -> f32 {
         self.layout_reflow(QueryMsg::CurrentCSSZoomQuery);
         self.layout
diff --git a/components/script_bindings/codegen/Bindings.conf b/components/script_bindings/codegen/Bindings.conf
index 8306d902192..6d04c0e7937 100644
--- a/components/script_bindings/codegen/Bindings.conf
+++ b/components/script_bindings/codegen/Bindings.conf
@@ -1090,6 +1090,10 @@ DOMInterfaces = {
     'cx': ['Supports', 'Supports_'],
 },
 
+'SVGGeometryElement': {
+    'cx': ['GetPointAtLength'],
+},
+
 'SVGGraphicsElement': {
     'cx': ['GetBBox'],
 },
diff --git a/components/script_bindings/webidls/SVGGeometryElement.webidl b/components/script_bindings/webidls/SVGGeometryElement.webidl
index b5a50b26330..68abc08c57f 100644
--- a/components/script_bindings/webidls/SVGGeometryElement.webidl
+++ b/components/script_bindings/webidls/SVGGeometryElement.webidl
@@ -7,8 +7,9 @@
 interface SVGGeometryElement : SVGGraphicsElement {
   //[SameObject] readonly attribute SVGAnimatedNumber pathLength;
 
-  //boolean isPointInFill(optional DOMPointInit point = {});
-  //boolean isPointInStroke(optional DOMPointInit point = {});
+  boolean isPointInFill(optional DOMPointInit point = {});
+  boolean isPointInStroke(optional DOMPointInit point = {});
   float getTotalLength();
-  //DOMPoint getPointAtLength(float distance);
+  [NewObject]
+  DOMPoint getPointAtLength(float distance);
 };
diff --git a/components/shared/layout/lib.rs b/components/shared/layout/lib.rs
index 785df46d4ce..debb26d512f 100644
--- a/components/shared/layout/lib.rs
+++ b/components/shared/layout/lib.rs
@@ -207,6 +207,22 @@ pub struct SVGGeometry {
     pub total_length: f32,
 }
 
+/// A point-in-shape question, for `isPointInFill`/`isPointInStroke`.
+#[derive(Clone, Copy, Debug)]
+pub enum SVGPointQuery {
+    InFill { x: f32, y: f32 },
+    InStroke { x: f32, y: f32 },
+    /// `getPointAtLength`.
+    AtLength(f32),
+}
+
+/// What [`SVGPointQuery`] resolved to.
+#[derive(Clone, Copy, Debug)]
+pub enum SVGPointAnswer {
+    Contains(bool),
+    Point { x: f32, y: f32 },
+}
+
 /// The address of a node known to be valid. These are sent from script to layout.
 #[derive(Clone, Copy, Debug, Eq, PartialEq, Hash)]
 pub struct TrustedNodeAddress(pub *const c_void);
@@ -424,6 +440,13 @@ pub trait Layout {
     /// everything below a direct child of the `<svg>` — `svg > *`'s
     /// `display: none` prunes the style traversal there.
     fn query_svg_geometry(&self, node: TrustedNodeAddress) -> Option<SVGGeometry>;
+
+    /// Answers a point query against an SVG shape, in its own user space.
+    fn query_svg_point(
+        &self,
+        node: TrustedNodeAddress,
+        query: SVGPointQuery,
+    ) -> Option<SVGPointAnswer>;
     fn stylist_mut(&mut self) -> &mut Stylist;
 
     /// Set whether the accessibility tree should be constructed for this Layout.
